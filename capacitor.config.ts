import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gentleraccoonnudge',
  appName: 'gentle-raccoon-nudge',
  webDir: 'dist'
};

export default config;
